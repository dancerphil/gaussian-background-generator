import {GaussianBackground as gaussianBackground} from './gaussianbackground';
import type {GaussianBackground, Layer, Options, BlurMethod} from './type';

export type {GaussianBackground, Layer, Options, BlurMethod};

export default gaussianBackground as unknown as GaussianBackground;
